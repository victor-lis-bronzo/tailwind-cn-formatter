# Tailwind cn() Formatter

VS Code extension scaffold for formatting Tailwind `cn()` class strings.

## Tech stack
VS Code Extension API, TypeScript, esbuild

## Running
```
npm install
npm run compile
```
Press F5 in VS Code to launch the extension in a development host.
